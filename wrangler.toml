name = "designer-web-app"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# Pages deployment is configured via open-next.config.ts
# Run `pnpm build && wrangler pages deploy` to deploy
